const functionName = "Math Operations"

const addition = (a, b) => {
    return a+b;
}

const subtraction = (a, b) => {
    return a-b;
}

const multiplication = (a, b) => {
    return a*b;
}

module.exports = { functionName, addition, subtraction, multiplication}